{
  "name": "decorify",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "format": "prettier --check .",
    "preinstall": "npx only-allow pnpm",
    "build": "pnpm --filter ./packages/** build",
    "dev": "pnpm --filter ./packages/** --parallel --parallel dev",
    "test": "pnpm --filter ./packages/** --parallel --parallel test",
    "clean": "pnpm --filter ./packages/** --parallel --parallel clean",
    "changeset": "changeset"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "husky": "^9.1.7",
    "prettier": "^3.8.1",
    "typescript": "^6.0.3"
  }
}
