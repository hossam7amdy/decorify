# compiled output
dist
node_modules
*.tsbuildinfo

# coverage
lcov.info

# local agent tooling
.claude/worktrees

# SDD scratch (not source; source is the single source of truth)
.superpowers/
